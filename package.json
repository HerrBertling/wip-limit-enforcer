{
  "name": "wip-limit-enforcer",
  "version": "1.0.0",
  "description": "",
  "author":
    "Markus Siering <markus277@gmail.com> (https://www.markussiering.com)",
  "license": "ISC",
  "repository": "https://github.com/HerrBertling/wip-limit-enforcer.git",
  "scripts": {
    "dev": "nodemon --exec \"npm start\"",
    "start": "probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest && standard",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage"
  },
  "dependencies": {
    "joi": "^13.2.0",
    "probot": "^6.0.0"
  },
  "devDependencies": {
    "jest": "^22.4.3",
    "nodemon": "^1.17.2",
    "smee-client": "^1.0.1",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">= 8.3.0"
  },
  "standard": {
    "env": ["jest"]
  }
}
